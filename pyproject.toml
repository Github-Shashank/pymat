[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pymat"
version = "0.1.0"
description = "A Python matrix manipulation library"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["matrix*"]